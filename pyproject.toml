[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "certificates-generator"
version = "0.0.1"
description = "Certificate Generator app"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
